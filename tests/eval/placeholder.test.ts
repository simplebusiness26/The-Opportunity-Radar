import { describe, expect, it } from 'vitest';

/**
 * The evaluation suite (10 scenario corpora x 6 evaluated behaviours) arrives
 * with the AI layer in Phase 5. This placeholder keeps the CI stage wired so
 * the suite cannot be forgotten, and states plainly that it is not yet real.
 */
describe('evaluation suite', () => {
  it('is not implemented until the AI layer exists (Phase 5)', () => {
    expect(true).toBe(true);
  });
});
